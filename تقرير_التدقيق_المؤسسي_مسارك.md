# 🏛️ تقرير التدقيق المؤسسي الشامل — منصّة مسارك
### Enterprise Audit & Redesign Blueprint — Masarak Platform
**التاريخ:** 27 حزيران 2026 · **النطاق:** المنصّة كاملة (10 مراحل) · **المنهجية:** قراءة الكود الفعلي + introspection حيّ لقاعدة Supabase + بناءً على تدقيقَين سابقَين موثّقَين.

> **ملاحظة منهجية مهمّة (صدق التقرير):** هذا تقرير **تدقيق + مخطّط إعادة تصميم + خطّة تنفيذ مرحلية**. ليس ادّعاءً بأنّ كل المنصّة أُعيد بناؤها في جلسة واحدة. ما **نُفِّذ فعلاً** موثّق في قسم منفصل (العمود الفقري للصلاحيات P0 + إصلاحات المؤسسات)، وما **يُقترَح** موثّق كخطّة دفعات صغيرة قابلة للتحقّق. التنفيذ الكامل عمل عدّة "sprints" ويبدأ بعد موافقتك على هذا التقرير.

---

## 📊 لوحة النتائج (Scorecard)

| المحور | التقييم | الاتجاه | الخلاصة بسطر |
|---|:---:|:---:|---|
| 🏗️ **Architecture** (المعماريّة) | **6.0 / 10** | ↗️ | تحسّنت بعد توحيد العمود الفقري للصلاحيات، لكن يبقى تشظٍّ schema وثلاث مفردات أدوار. |
| 🎨 **UX** (تجربة المستخدم) | **5.5 / 10** | → | spinners لا نهائية، لا loading states، نوافذ `alert()` أصلية غير عربية، تناسق ضعيف. |
| ⚡ **Performance** (الأداء) | **5.0 / 10** | → | خط Tajawal مُحمَّل مرّتين، قاموس i18n عملاق على كل صفحة، قوائم تُرسَم client-side. |
| 🔒 **Security** (الأمان) | **6.5 / 10** | ↗️ | RLS مفعّل 72/72 + صلاحيات وُحِّدت، لكن حقن إشعارات + دوال anon + لا rate-limiting. |
| 📈 **Scalability** (قابلية التوسّع) | **6.0 / 10** | ↗️ | أساس قاعدة بيانات عالمي ممتاز، لكن طبقة الواجهة مقفلة على لبنان. |
| ♿ **Accessibility** (الوصوليّة) | **5.0 / 10** | → | نقص `aria-*`، تباين ألوان يسقط في WCAG AA، نوافذ أصلية غير RTL. |
| 🔍 **SEO** | **6.0 / 10** | → | أساس متادتا/sitemap جيّد، لكن مقالات بلا metadata، روابط 404، لا hreflang، slugs لبنانية. |
| 🔧 **Maintainability** (الصيانة) | **4.5 / 10** | → | لا اختبارات/CI، تكرار وحدات، 86 `any`، تشظّي SQL خطير. |
| 🧩 **Product** (المنتج) | **6.0 / 10** | → | مزايا غنيّة لكن كثير منها dead/غير موصول؛ أحداث تحليلية لا تُطلَق؛ بوّابة الراعي مفقودة. |
| 💰 **Business** (الأعمال) | **5.5 / 10** | ↗️ | قمع التربّح (leads) كان معطّلاً (أُصلح)، Stripe غير موصول، لكن موقع سوقي قوي. |
| | | | |
| **🎯 التقييم الإجمالي** | **⭐ 5.6 / 10** | ↗️ | منصّة غنيّة بإمكانات هائلة وأساس أمني/بياناتي سليم، تحتاج إنضاجاً هندسيّاً وتحرّراً من قيد لبنان. |

---

## 🧭 الملخّص التنفيذي (للقراءة على الهاتف)

**مسارك في جملة واحدة:** منتج طموح وغنيّ بالمزايا، مبنيّ على أساس بيانات عالمي ممتاز وأمان RLS متين، لكنه **مقفل على لبنان في طبقة الواجهة** و**يفتقر للنضج الهندسي** (لا اختبارات، لا مراقبة، تشظٍّ في الكود).

**أهم 6 قرارات استراتيجية:**

1. 🔴 **حرّر المنصّة من قيد لبنان فوراً.** قاعدة البيانات جاهزة عالميّاً (جدول `countries` فيه 28 دولة، `universities_global` 222 جامعة) — لكن الواجهة تكتب `setCountry("لبنان")` وتعرض `+961` ونظام البكالوريا اللبناني للجميع. **35 موضعاً يحجب الاستخدام العربي.** الحل ليس معماريّاً بل تهيئة (configuration).

2. 🔴 **حوّل لوحة الأدمن إلى "غرفة قيادة الرئيس التنفيذي" (CEO Command Center).** اللوحة الحالية تشغيليّة ممتازة لكنها تفتقر مقاييس القرار: **MRR/ARR موجود، لكن لا churn %, لا LTV, لا CAC, لا cohort retention, لا DAU/MAU, لا توزيع جغرافي.**

3. 🔴 **أغلِق الثغرات الأمنية القابلة للاستغلال** (حقن إشعارات التصيّد + دوال anon-executable + 5 جداول INSERT-true + لا rate-limiting).

4. 🟠 **أوقِف "الكود الميّت المضلِّل":** 10 من 16 حدثاً تحليليّاً لا تُطلَق أبداً → لوحات القيادة تعرض أصفاراً مضلِّلة. وحدات وملفات tabs ميّتة. مقاييس "—".

5. 🟠 **ابنِ شبكة الأمان الهندسية:** Vitest + GitHub Actions CI + Sentry. منصّة فيها مدفوعات وRLS وcron بلا أي اختبار أو رؤية أعطال = خطر وجودي.

6. 🟢 **سدّ فجوات المنتج:** بوّابة الراعي (Sponsor) معرّفة كدور لكن بلا واجهة؛ لوحات المؤسسات (جامعة/مدرسة/مهني) تتشارك سطحاً واحداً.

---

# 🔍 المرحلة 1 — تدقيق لوحة الأدمن الأعلى + إعادة التصميم

## 1.1 الوضع الحالي (مدقّق widget-by-widget)

اللوحة فيها **23 تبويباً حيّاً** عبر `src/app/admin/dashboard/page.tsx` و`_tabs/`. التقييم العام: **B+ في المتانة التشغيليّة / C- في رؤية القرار التنفيذي.**

**ما يعمل جيّداً (KEEP):**
- **Executive Overview** — 6 بطاقات KPI أساسيّة (مستخدمون، نشطون 30 يوم، تسجيل اليوم، إيراد اليوم/الشهر/السنة) موصولة بـ `admin_kpi_overview()` RPC. ✅
- **Audit Log** — سجلّ تدقيق ممتاز: غير قابل للتعديل، قابل للاستعلام، بتسميات واضحة. ✅
- **Subscriptions / Revenue** — MRR/ARR/ARPU + توزيع الخطط + churn count. مَحْميّة من الـ co-admins بشكل صحيح. ✅
- **Career DNA Analytics** — توزيع RIASEC الستّة + إحصاءات الاعتماد. ✅
- **Marketing Center** — قمع التحويل + إسناد UTM (مصادر/حملات/أجهزة). ✅
- **Admins Manager** — منح/سحب co-admin عبر RPC، محصور بالـ super-admin. ✅

**ما يجب إصلاحه/حذفه:**

| البند | الموقع | القرار | السبب |
|---|---|:---:|---|
| **`UsersTab.tsx`** | `_tabs/UsersTab.tsx` | 🗑️ **REMOVE** | tab يتيم — معرّف لكن غير مستورَد في `page.tsx`؛ نسخة قديمة مكرّرة من `StudentsCenterTab`. |
| **بطاقة عدد فريق العمل** | `ExecutiveOverviewTab.tsx:217` | ⚠️ **DEAD** | تعرض `"—"` ثابتة (لا بيانات) — إمّا تُوصَل بعدّ `team_members` أو تُحذف. |
| **توقّعات الإيرادات** | `RevenueCenterTab.tsx:96-104` | 🔧 **REDESIGN** | إسقاط خطّي ساذج (+5% شهريّاً مفترَض) قد يُظَنّ تنبّؤ ML — يجب تسميته "إسقاط خطّي بسيط". |
| **بطاقات "Coming Soon"** | 5 تبويبات | ➡️ **MOVE** | افصلها في تبويب "خارطة طريق المنتج" واحد بدل تشتيتها. |
| **درجة التفاعل (Engagement)** | `StudentsCenterTab.tsx:134` | 🔧 **REDESIGN** | الأوزان (30 نقطة لإكمال DNA) مبهمة — اجعلها قابلة للتهيئة أو وثّقها. |

## 1.2 الاكتشافات الحرجة

🚨 **خرق مبدأ أقلّ-صلاحية (Least Privilege):** تبويب `StudentsCenterTab` يكشف **بيانات شخصيّة (PII)** كاملة (الاسم، الإيميل، الصف، المدينة، آخر دخول) **للـ co-admins** — بينما تبويبات الإيرادات مَحْجوبة عنهم. خطأ منطقي: بيانات الطلاب لا تقلّ حساسيّة عن الإيرادات.
- **الحل:** أضِف `'students'` إلى `CO_ADMIN_HIDDEN` (`page.tsx:103`)، أو جرّد الـ PII من رؤية الـ co-admin.

🚨 **عدم تطابق الأحداث التحليليّة:** `src/lib/analytics.ts` يعرّف 16 نوع حدث، **لكن 10 لا تُطلَق أبداً** (`subscription_started/canceled`, `login/logout`, `newsletter_subscribe`, `start_quiz`, `support_ticket_created`...). النتيجة: **قمع التحويل وchurn يعرضان أرقاماً ناقصة/مضلِّلة.**

🚨 **افتراض عملة واحدة:** كل حسابات MRR/ARR تفترض USD بلا تحويل عملات — قاتل عند التوسّع العربي متعدّد العملات.

## 1.3 المقاييس المفقودة في "غرفة قيادة الرئيس التنفيذي"

اللوحة الحاليّة **تشغيليّة** لا **استراتيجيّة**. ينقصها للقرار:
- **معدّل التسرّب % (Churn Rate)** — موجود كعدد فقط لا كنسبة/اتجاه.
- **القيمة الدائمة للعميل (LTV)** و**تكلفة الاكتساب (CAC)** وزمن استرداده.
- **بقاء الأفواج (Cohort Retention)** — كم من مستخدمي شهر 1 بقوا نشطين في شهر 2.
- **DAU / MAU** (نشاط يومي مقابل شهري) + نسبة اللزوجة (stickiness).
- **تفصيل قمع التفعيل (Activation Funnel)** — % إتمام التحقّق → DNA → أوّل حفظ → اشتراك.
- **التوزيع الجغرافي** — مستخدمون/إيرادات حسب الدولة (حيوي للتوسّع العربي).
- **إسناد القناة → الاشتراك** — أيّ `utm_source` يحوّل فعلاً لمشترك مدفوع.

## 1.4 التصميم الجديد المقترَح — "CEO Command Center"

**هيكل من 4 طبقات بدل 23 تبويباً مسطّحاً:**

```
الطبقة 1 — النبض (Pulse): شريط علوي دائم
  └─ MRR/ARR · النموّ الصافي · Churn% · MAU · Runway

الطبقة 2 — القرار (Decision Center): الافتراضي عند الدخول
  ├─ قمع التفعيل الحيّ (مع نِسَب التسرّب الحقيقيّة)
  ├─ بقاء الأفواج (heatmap شهري)
  ├─ التوزيع الجغرافي (خريطة عربيّة + إيراد/دولة)
  └─ موجز Masarak Intelligence (AI) — "أهمّ 3 قرارات اليوم"

الطبقة 3 — مراكز التشغيل (Operate): مفلترة بالصلاحية
  └─ Students · Content · Support · Marketing · SEO · Audit
     (كلّ co-admin يرى مراكزه فقط — أقلّ صلاحية)

الطبقة 4 — المال (Money): super-admin فقط
  └─ Revenue · Subscriptions · Sponsors (مع تحويل عملات)
```

**المبدأ:** لا شيء يبقى لمجرّد وجوده. القياس الافتراضي = **القرار**، لا الجدول.

---

# 🌍 المرحلة 2 — التحرّر من قيد لبنان (Pan-Arab)

## 2.1 الخبر الجيّد: الأساس البياناتي جاهز عالميّاً ✅

- جدول `countries` (28 دولة: LB, SA, AE, QA, JO, EG, MA, TN...) — مصدر حقيقة واحد، قابل للتوسّع لـ 195 دولة.
- `universities_global` (222 جامعة) و`scholarships_global` متعدّدة الدول بالكامل، مع `country_code` FK و metadata ثنائيّة اللغة.
- التوجيه (Routing) جاهز: `/universities/country/{slug}`, `/study-abroad/{country}` — والـ sitemap يولّد المسارات ديناميكيّاً من قاعدة البيانات.

## 2.2 الخبر السيّئ: طبقة الواجهة مقفلة على لبنان 🔴

**35 موضعاً يحجب الاستخدام العربي.** أبرزها:

| الفئة | المثال | الموقع | الأثر |
|---|---|---|---|
| **النطاق/الإيميل** | `masaraklb.com`, `support@masaraklb.com` | `seo.ts:10`, `next.config.mjs`, +20 صفحة | سعوديّ يرى روابط "lb" → ارتباك ثقة + SEO. |
| **الهاتف** | `+961 70 000 000` | 5 ملفات (CV, sponsors, profile...) | يفترض رقماً لبنانيّاً للجميع. |
| **نظام التعليم** | `Grade 10 / EB1`, أقسام بكالوريا `GS/LS/SE/LH`, علامة من 20 | `AcademicTab.tsx:40-51`, onboarding | طالب سعوديّ/مصريّ يرى مصطلحات لا معنى لها. |
| **التعليم المهني** | مستويات `LT/BT/TS` + رواتب `900–1300$` + 10 معاهد لبنانيّة | `vocational/data.ts` | صفر محتوى مهني لغير لبنان. |
| **المدن/المناطق** | `LEBANON_PROVINCES` (بيروت، جبل لبنان...) | `org/request-access:24` | لا محافظات مصريّة ولا مناطق سعوديّة. |
| **الافتراضات** | `setCountry("لبنان")` | `PartnershipForm.tsx:20` | كل نموذج يبدأ من لبنان. |
| **المحتوى/SEO** | `"Lebanese universities"` في 50+ موضع | i18n + slugs | نصوص أبطال + روابط مقفلة على لبنان. |
| **الذكاء الاصطناعي** | `"Lebanese Arabic... for Lebanese students"` | `ai.ts:90` | المستشار المهني ينصح بسياق لبناني للسعوديّين. |

## 2.3 المعماريّة المقترَحة: نظام واعٍ بالدولة (Country-Aware)

**مصدر حقيقة واحد، صفر تشفير صلب للدول:**

```
1) countries (قاعدة البيانات)  ← موجود ✅ — الأمّ لكل شيء
2) country-config.ts (NEW)      ← per-deployment: نطاق، إيميل، prefix هاتف، عملة
3) education-systems.ts (NEW)   ← خريطة: دولة → صفوف/أقسام/سُلّم علامات
4) useCountry() hook (NEW)      ← سياق React يختار تلقائيّاً حسب النطاق/الموقع
5) جداول جديدة: regions, vocational_tracks, schools (country_code FK)
```

**الطريق الحرج لإطلاق السعوديّة:** تهيئة البيئة (2س) + نظام تعليم (6س) + مكوّن هاتف (3س) + ترحيل بيانات مهنيّة (8س) + محتوى متغيّر (8س) + اختبار (8س) ≈ **35 ساعة** → السعوديّة قابلة للإطلاق خلال **5–7 أسابيع**، ثم مصر/الإمارات/المغرب (2–3 أسابيع لكلّ، بجهد متناقص).

---

# 🧱 المراحل 3–9 — التدقيق الشامل (مكثّف)

> هذه المراحل تبني على التدقيقَين السابقَين (`AUDIT_REPORT.md` 8 محاور، و`ADMIN_ARCHITECTURE_AUDIT.md` للأدوار). أبرز ما يبقى مفتوحاً:

## المرحلة 3 — تدقيق المنصّة (صفحات/APIs/قاعدة بيانات)
- 🔴 **C-1 (أُصلح):** قمع leads المؤسسات كان كوداً ميّتاً (قراءة عمود `universities.org_id` غير موجود) → جوهر التربّح معطّل.
- 🔴 **dashboards تعلّق للأبد** عند فشل التحميل (spinner بلا try/catch ولا error.tsx).
- 🟠 صفحات القوائم (جامعات/منح/تخصّصات) تُرسَم client-side خلف spinner → LCP بطيء.
- 🟡 **24 FK بلا index** + 35 index غير مستخدم + 101 `auth_rls_initplan`.

## المرحلة 4 — تدقيق المنتج (تحدّي كل ميزة)
- 10 من 16 حدثاً تحليليّاً لا تُطلَق → مزايا "وهميّة" تقيس الصفر.
- بوّابة الراعي (Sponsor) دور بلا واجهة. لوحات المؤسسات الثلاث تتشارك سطحاً واحداً.
- أداة "معادلة البكالوريا" لبنانيّة بحتة — عبء ميّت لغير اللبنانيّين.

## المرحلة 5 — تدقيق UX
- ~45 موضع `alert()/confirm()` أصلي (غير عربي، غير RTL، يحجب الواجهة) — يحتاج نظام toast.
- 0 `loading.tsx`، 0 `global-error.tsx` → خطأ في الـ root layout = صفحة Next إنجليزيّة (صادم في منتج RTL).
- ~33 `<img>` خام (لا `next/image`) → خطر CLS على بطاقات الجامعات.
- `ink-subtle (#9CA3AF)` يسقط في WCAG AA (~2.8:1)؛ أزرار dropdown بلا `aria-expanded`.

## المرحلة 6 — تدقيق الهندسة
- 🔴 **لا اختبارات، لا CI، لا مراقبة أخطاء** (Sentry معلَّق بتعليقات فقط).
- 🔴 **تشظّي schema:** 3 نماذج هويّة + README يثبّت النسخة الخطأ التي تُلغي إصلاح التسجيل.
- 🟡 86 `: any`، تكرار وحدتَي حفظ (`saved.ts` مقابل `savedItems.ts`)، `toggleSave` غير ذرّي.
- خط Tajawal مُحمَّل **مرّتين** (`<link>` + CSS `@import`) + قاموس i18n (4413 سطر) على **كل** صفحة.

## المرحلة 7 — تدفّق البيانات
- كل الـ side-effects (إشعارات/leads) تعمل **client-side** best-effort → تُفقد إن أُغلق التبويب. محلّها الصحيح DB triggers أو API routes.
- إشعارات مكرّرة (لا dedup key) — إعادة اختبار DNA تُطلِق إشعاراً في كل مرّة.

## المرحلة 8 — تحقّق اللوحات المتقاطع
- ✅ لا تسريب بيانات عبر-المستأجِر (org tables محميّة بـ RLS). 
- 🚨 خطر التعرّض الحقيقي = **مراكز الأدمن**: تأكّد أن كل `*CenterTab` يستعلم بـ RLS لا اعتماداً على "بوّابة الإيميل عبرت".

## المرحلة 9 — اكتشاف المزايا (مقترحات)
- **AI:** كاشف المخاطر للمؤسّسات، تنبّؤ churn، توصيات مسار ذكيّة، موجز أسبوعي تلقائي.
- **تحليلات:** cohort retention engine، خريطة حرارة جغرافيّة، تتبّع spend → CAC.
- **تربّح:** إكمال Stripe + webhooks، باقات للمؤسسات، إعلانات مموَّلة للجامعات.
- **تكامل:** Google Search Console، WhatsApp Business API، بوّابة دفع محليّة (سعوديّة/إماراتيّة).

---

# ✅ المرحلة 10 — ما نُفِّذ فعلاً (هذه الجلسة والجلسات السابقة)

> صدقاً وبدقّة: التالي **مُنجَز ومُتحقَّق منه** على فرع `feat/scholarship-filters`. الباقي خطّة تنتظر موافقتك.

1. ✅ **العمود الفقري للصلاحيات (P0):** نقل الـ role من `user_metadata` (قابل للتعديل من المستخدم) إلى `app_metadata` (server-only). الإيميل المالك صار في **مكان واحد فقط** (`is_super_admin()`) بدل ~48 سياسة + 5 دوال.
2. ✅ **طبقة co-admin حيّة** مع تقسيم RLS (8 جداول ماليّة/PII تبقى super-only).
3. ✅ **تصلّب أمني:** سحب EXECUTE من PUBLIC للدوال؛ cron clients صارت fail-closed؛ middleware أسقط استعلام `org_members` لكل طلب.
4. ✅ **إصلاحات المؤسسات:** فيديوهات Campus-life تظهر؛ رفع شعار/بانر من الجهاز؛ شارة التوثيق على بطاقات القوائم؛ طلبات الانتساب تصبح leads عبر DB trigger؛ تبويب منح + أخبار جديد.

---

# 📋 الخلاصة المنظّمة

### 🔧 ما أُصلح
- العمود الفقري للصلاحيات (تصعيد role + إيميل مشفّر) — **مُغلَق ومُتحقَّق**.
- تصلّب أمني (PUBLIC grants، cron، middleware).
- 6 أعطال في لوحة المؤسسات (فيديو، شعار، شارة، leads، أخبار، زرّ رجوع).

### 🗑️ ما يُحذف (مقترَح)
- `UsersTab.tsx` (يتيم)، tabs ميّتة (`UniversitiesTab/SchoolsTab/DashboardTab`)، عضو `careers_center` غير المستخدَم.
- ملفات SQL المتعارضة (`supabase-schema.sql`, `schema.sql`, `v2_schema.sql`) — توحيد على `migrations/`.
- سكربتات `.bat/.ps1` قديمة، `next.config.mjs` الفارغ المكرّر.

### 🎨 ما يُعاد تصميمه (مقترَح)
- لوحة الأدمن → "CEO Command Center" بـ 4 طبقات (نبض/قرار/تشغيل/مال).
- توقّعات الإيرادات (تسمية صادقة)، درجة التفاعل (أوزان شفّافة)، ماسح SEO (6–8 فحوص).
- صفحات القوائم → Server Components + ISR.

### ➕ ما يُضاف (مقترَح)
- نظام واعٍ بالدولة (`country-config` + `education-systems` + `useCountry` + جداول regions/vocational).
- مقاييس CEO (Churn%, LTV, CAC, cohort, DAU/MAU, جغرافيا).
- بوّابة الراعي + لوحات المؤسسات حسب النوع.
- Vitest + GitHub Actions CI + Sentry + rate-limiting + نظام toast.

### 🔮 توصيات مستقبليّة
- إكمال Stripe + webhooks (شرط قبل الاعتماد على مقاييس الاشتراك للتقارير).
- محرّك أحداث مركزي (`emitEvent`) يغذّي: projections + analytics + notifications + dashboards.
- توليد أنواع Supabase وتقليل الـ `any`؛ hreflang للنسخة الإنجليزيّة.

---

## 🗺️ خطّة التنفيذ المقترَحة (دفعات صغيرة مُتحقَّقة)

**الأسبوع 1 — أوقِف النزيف (Critical + أمان):** أغلِق حقن الإشعارات + دوال anon + جداول INSERT-true؛ try/catch للـ dashboards؛ search_path للدوال.

**الأسبوع 2 — الاستقرار:** `next/font` + تقسيم i18n؛ توحيد schema؛ Sentry + CI + أوّل دفعة Vitest؛ أطلِق الأحداث الناقصة.

**الأسبوع 3–4 — التحرّر من لبنان:** `country-config` + `useCountry` + نظام تعليم ديناميكي + ترحيل البيانات → **إطلاق السعوديّة**.

**الشهر 2 — العمق:** CEO Command Center؛ بوّابة الراعي؛ Server Components + ISR؛ إصلاح RLS perf + indexes.

---

*انتهى التقرير. تدقيق مبنيّ على قراءة الكود الفعلي + introspection حيّ لـ Supabase + تدقيقَين موثّقَين سابقَين. التنفيذ الكامل يبدأ بعد موافقتك، بدفعات صغيرة يُتحقَّق من التطبيق بعد كلٍّ منها لتفادي الأعطال.*
