/**
 * DEPRECATED — all config has been consolidated into next.config.ts
 * (Next.js 14+ uses .ts when both files exist)
 * This file is kept empty to avoid confusion.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};
export default nextConfig;
