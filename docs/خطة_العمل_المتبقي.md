# 🛠️ مسارك — خطّة العمل المتبقّي (فحص شامل + مراحل)
**بتاريخ 2026-06-29 — مبنيّ على فحص دقيق (3 فرق فحص متوازية) + ملاحظات المستخدم المباشرة.**

---

## أولاً: المشاكل المؤكَّدة (بجذرها وموقعها)

| # | المشكلة | الجذر | الموقع | الخطورة |
|---|---|---|---|---|
| 1 | **المنح → 404** عند فتح منحة | لا توجد صفحة تفصيل `/scholarships/[slug]` والـslugs فاضية | `scholarships/page.tsx` (لا `[slug]`) | 🔴 حرِج |
| 2 | **اللوحة غير متناسقة على الهاتف** | شريط الأدمن الجانبي `w-72` (288px) ما بينطوي → المحتوى ينعصر ~92px | `admin/dashboard/page.tsx:126` | 🔴 حرِج |
| 3 | **ما بقدر أضغط "إضافة عضو" على الهاتف** | على الأرجح نتيجة #2 (الزرّ يعمل على الديسكتوب) | `admin/.../TeamTab.tsx:135` | 🔴 حرِج |
| 4 | **لا رفع صورة لعضو الفريق** | النموذج فيه إيموجي + لصق رابط فقط، ما في `input type=file` | `TeamTab.tsx:165-166` + `handleSave:81` | 🟠 عالٍ |
| 5 | **مشاركة الملف ما بتشتغل** | الزرّ ينسخ `/profile` (خاص، يتطلّب دخول) بدل الرابط العام `/student/[masarakId]` | `profile/page.tsx:234` | 🟠 عالٍ |
| 6 | **الجامعات: لبنان فقط + "ملخّص سريع"** | `/universities` يقرأ الجدول القديم (كرت لبنان فقط)؛ الـ234 العالميّة مخفيّة خلف صفحات الدول؛ وقائمة لبنان تقرأ بيانات ثابتة (23 لا 35) | `universities/page.tsx:30-36` + `universities/lebanon` | 🟠 عالٍ |
| 7 | **ترتيب جامعات لبنان مقلوب (35→1)** | الترتيب الافتراضي تنازلي بالـrank (`rb - ra`) | `universities/lebanon/page.tsx:66` | 🟡 متوسط |
| 8 | حقول البحث بالأدمن تطفح على الهاتف | `min-w-[200px]` بلا breakpoint | عدّة tabs (Students/Scholarships/Schools/Universities…) | 🟡 متوسط |
| 9 | شبكات KPI + جداول الأدمن تطفح أفقيّاً على الهاتف | grids بلا breakpoints + جداول 7-8 أعمدة | `ExecutiveOverviewTab` + جداول | 🟡 متوسط |
| 10 | دوائر الـblur تطفح + اتجاه RTL خاطئ | `w-96/-right-32` ثابتة | `dashboard/page.tsx:165-166` | 🟢 منخفض |

> ملاحظة: «#10 أدلّة دراسة-بالخارج» جاهزة بس **غير مدموجة** (فرع `feat/study-abroad-guides`).

---

## ثانياً: خطّة المراحل (مرتّبة بالأثر على المستخدم)

### 🔴 المرحلة 1 — إصلاحات حرِجة يراها المستخدم (أولاً)
1. **صفحات تفصيل المنح** `/scholarships/[slug]` (+ توليد slugs + metadata SEO) → يحلّ الـ404 ويعطي كل منحة صفحة حقيقيّة.
2. **تجاوب لوحة الأدمن على الهاتف** — تحويل الشريط الجانبي لقائمة منزلقة (drawer) أو أعلى الصفحة على الموبايل؛ يحلّ «اللوحة منعصرة» و«ما بقدر أضغط إضافة عضو».
3. **رفع صورة عضو الفريق** — `input type=file` + رفع لـ Supabase Storage (نفس نمط صورة الملف الشخصي).
4. **إصلاح مشاركة الملف** — نسخ/مشاركة الرابط العام `/student/[masarakId]` مع تنبيه لتفعيل الرؤية العامّة أولاً.
5. **ترتيب جامعات لبنان 1→35** (تصاعدي).

### 🟠 المرحلة 2 — قسم الجامعات (تموضع عربي حقيقي)
6. **إظهار كل الدول + الـ234 جامعة عالميّة على `/universities`** (لا لبنان فقط)، وتوحيد بيانات لبنان (23 ثابتة مقابل 35 بقاعدة البيانات).
7. تحسين تصفّح الجامعات حسب الدولة + ربط داخلي.

### 🟡 المرحلة 3 — بقيّة تجاوب الموبايل + تلميع
8. حقول البحث (`min-w` → `sm:min-w`)، شبكات KPI breakpoints، إخفاء أعمدة الجداول الثانويّة على الهاتف، دوائر الـblur + RTL، تباعد الـbottom-nav.
9. لوحة الطالب «لوحتي» على الهاتف (بطاقة الترحيب + الشبكات).

### 🟢 المرحلة 4 — أدوات القرار والرحلة (من الاستراتيجيّة)
10. فاحص أهليّة المنح (يحتاج إضافة حقول للمنح: معدّل/جنسيّة/مستوى/IELTS).
11. الصفحة الرئيسيّة القراريّة + محور «ابدأ رحلتك» + أداة مطابقة الدولة.
12. دمج أدلّة الدراسة-بالخارج العشرة (الفرع الجاهز).

### ⚙️ المرحلة 5 — تصلّب هندسي (من تدقيق 06-24)
13. الأداء: توحيد الخطّ (next/font) + تقسيم i18n الضخم.
14. شبكة أمان: Vitest + GitHub Actions CI + Sentry.
15. الوصوليّة (تباين الألوان + aria).
16. توحيد الـschema المتبقّي (تقاعد جدول `profiles`).

### 💰 المرحلة 6 — نموّ وتربّح
17. مزوّد دفع إقليمي (يدعم لبنان) + تفعيل الاشتراكات.
18. بوّابة الراعي + CEO Command Center (لمّا يصير في بيانات/إيرادات).
19. التوسّع الكامل للمحتوى (600 مقال) + محور الدعم العاطفي.

---

## ثالثاً: طريقة التنفيذ
- كل مرحلة = دفعات صغيرة، كل دفعة: تعديل → `tsc` → فرع → معاينة → دمج بإذنك.
- التعديلات التي تمسّ قاعدة البيانات (slugs المنح، حقول الأهليّة) = SQL آمن تشغّله أنت بلوحة Supabase (محظور عليّ مباشرةً).
- لا نشر للإنتاج إلا بدمجك للـ PR.

**الاقتراح:** نبدأ بالمرحلة 1 فوراً (أعلى أثر يراه المستخدم)، بند-بند مع معاينة.
